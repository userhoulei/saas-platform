<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.saasplatform.mapper.system.SysRoleMenuMapper">

    <insert id="batchInsert">
        INSERT INTO sys_role_menu (role_id, menu_id, tenant_id, create_time, update_time, deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.menuId}, #{item.tenantId}, NOW(), NOW(), 0)
        </foreach>
    </insert>

</mapper>